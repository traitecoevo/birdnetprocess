% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon_groups.R
\name{group_colours}
\alias{group_colours}
\title{Colours for taxon groups}
\usage{
group_colours(rules)
}
\arguments{
\item{rules}{A named list of grouping rules, or a \code{birdnet_deployment}.}
}
\value{
A named character vector of colours, one per group.
}
\description{
Pulls the per-group \code{colour} entries out of a set of grouping rules into a
named vector suitable for \code{\link{plot_daily_activity}}'s
\code{group_colours} argument. Groups without a declared colour fall back to the
package palette, so a config need only name the colours it cares about.
}
\details{
Group colours are drawn from the stricter all-pairs-safe palette (see
\code{\link{birdnet_palette}}), because groups usually appear as species
labels scattered down a figure where any two may be compared, not as
neighbouring bars.

A group named \code{"Other"} gets the muted residual grey unless the config says
otherwise: it is a leftover bin rather than a category anyone is meant to
track, and giving it a hue implies more than it means.
}
\seealso{
\code{\link{assign_groups}}, \code{\link{birdnet_palette}}
}
