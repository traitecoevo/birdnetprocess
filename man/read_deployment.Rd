% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deployment.R
\name{read_deployment}
\alias{read_deployment}
\title{Read a deployment configuration file}
\usage{
read_deployment(path)
}
\arguments{
\item{path}{Path to the YAML file, or to a directory containing a
\code{deployment.yml}.}
}
\value{
An object of class \code{birdnet_deployment}: a list with elements
\code{name}, \code{location}, \code{detector}, \code{sites}, and \code{analysis}, plus the
resolved \code{path} and \code{dir} of the file it came from. Each element of
\code{sites} gains \code{start_after} and \code{end_before} bounds derived from its
\code{date} (or \code{start}/\code{end}).
}
\description{
Reads a \code{deployment.yml} describing one field deployment - where the
recorders were, which sites and dates they covered, which detector produced
the detections, and how the results should be filtered and grouped. The
resulting object is the single input to
\code{\link{read_deployment_detections}} and \code{\link{birdnet_report}}.
}
\details{
The same file is intended to be read by the detector-running step (in
Python) as well as by this package, so that the site list and target dates
cannot drift apart between the two halves of the pipeline.

Relative paths in the file are resolved against the directory containing the
file itself, not the working directory, so a deployment folder can be moved
or checked out anywhere. A leading \code{~} is expanded.

See \code{\link{new_deployment}} to write a commented starter file.

The expected structure is:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{name: Smiths Lake phenology
location:
  latitude: -32.38
  longitude: 152.51
  tz: Australia/Sydney
  place: Smiths Lake, NSW
detector:
  name: pelican0-15
  model: path/to/pelican0-15.tflite
  labels: path/to/pelican0-15_Labels.txt
sites:
  - id: Powerline Strip
    detections: path/to/detections/Powerline Strip
    date: 2026-02-03
analysis:
  confidence: 0.5
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
cfg <- read_deployment("deployments/smiths_lake/deployment.yml")
cfg
}
}
\seealso{
\code{\link{new_deployment}},
\code{\link{read_deployment_detections}}
}
