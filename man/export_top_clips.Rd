% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_clips.R
\name{export_top_clips}
\alias{export_top_clips}
\title{Export the highest-confidence audio clips for each species}
\usage{
export_top_clips(
  data,
  cfg,
  output_dir,
  n_per_species = 5,
  pad = 1,
  species = NULL,
  n_species = NULL,
  flag_species = character(0)
)
}
\arguments{
\item{data}{Detections, e.g. from \code{\link{read_deployment_detections}}.
Needs \verb{Common Name}, \code{Confidence}, and either \verb{Begin Path} or
\code{file_name} to identify the source recording.}

\item{cfg}{A \code{birdnet_deployment}, used to find the recordings.}

\item{output_dir}{Folder to write clips into. Created if needed.}

\item{n_per_species}{Number of clips per species. Default \code{5}.}

\item{pad}{Seconds of context before and after the detection. Default \code{1}.}

\item{species}{Optional character vector limiting which species to export.}

\item{n_species}{Export only the \code{n_species} most-detected species. \code{NULL}
(the default) exports every species. Ignored if \code{species} is given.}

\item{flag_species}{Species to mark as unvalidated on the listening sheet and
sort to the top. Default none.}
}
\value{
A tibble describing every clip written, invisibly. Also written to
\code{clips.csv} in \code{output_dir}. The number of species in the report it
was drawn from is attached as \code{attr(x, "n_species_total")}.
}
\description{
Cuts the top-scoring detections of every species out of the source
recordings and writes them to a folder, one subfolder per species. This is
the fastest way to check whether a detector is right: a species table says a
bird was heard 200 times, and five clips say whether it was that bird.
}
\details{
Verification is the step a detection report cannot do for you, and the
friction of finding the audio by hand is usually what stops it happening. A
species' best clips are also its most flattering, so treat this as a
first-pass sanity check — if the top five are wrong, the species is wrong;
if they are right, the marginal ones still need listening to.

Clip filenames carry the confidence, site and timestamp, so a folder can be
skimmed without opening the manifest.

Source recordings are located by file name against each site's \code{recordings}
folder in the config, not by the path recorded in the detections. Detector
runs analyse a temporary folder of symlinks that is deleted afterwards, so
the path stored with a detection is generally dead by the time anyone wants
the audio.

Requires \code{ffmpeg} on the PATH.
}
\examples{
\dontrun{
cfg <- read_deployment("deployments/wild_deserts/deployment.yml")
d <- read_deployment_detections(cfg)
export_top_clips(d, cfg, "report/clips", n_per_species = 5)
}
}
