% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_range.R
\name{filter_by_range}
\alias{filter_by_range}
\title{Filter BirdNET detections to species likely present at a location}
\usage{
filter_by_range(
  data,
  abundance_raster,
  latitude,
  longitude,
  min_abundance = 0,
  name_overrides = NULL,
  species_col = "Common Name"
)
}
\arguments{
\item{data}{A tibble of BirdNET detections (e.g. from
\code{\link{read_birdnet_folder}}).}

\item{abundance_raster}{A \code{terra} \code{SpatRaster} of per-species abundance
layers, or a file path to one. Requires the \code{terra} package.}

\item{latitude, longitude}{Numeric. The recorder location, in decimal degrees
(WGS84 / EPSG:4326).}

\item{min_abundance}{Numeric. Species with a modelled abundance at or below
this value are also removed. Default \code{0} (range-only behaviour: keep
anything within the modelled range).}

\item{name_overrides}{Optional named character vector mapping common names to
layer names, for species the automatic matcher misses, e.g.
\code{c("Some Bird" = "some_bird_layer")}.}

\item{species_col}{Name of the column holding common names. Default
\code{"Common Name"}.}
}
\value{
The filtered \code{data} tibble. A per-species report is attached as
\code{attr(result, "range_report")}, a tibble with columns \code{species},
\code{n_detections}, \code{max_confidence} (the highest detection confidence
for that species), \code{matched_layer}, \code{abundance}, and \code{status}.
}
\description{
Removes detections of species whose modelled range does \strong{not} cover the
recorder's location, using a species-distribution-model (SDM) raster stack.
This is aimed at the geographic false positives that BirdNET commonly
produces (e.g. eastern/coastal species proposed at an arid inland site).
}
\details{
The abundance raster is expected to be a multi-layer stack with one
range-masked layer per species, named in lower-case underscore style
(e.g. \code{zebra_finch}, \code{noisy_miner}). A cell value of \code{NA} is interpreted as
"outside the modelled range" — the convention used by range-masked SDMs.

Each detected species is classified as:
\itemize{
\item \strong{kept: within modelled range} — a matching layer exists and the
site has a (non-\code{NA}) abundance value above \code{min_abundance}.
\item \strong{removed: outside modelled range} — a matching layer exists but the
site value is \code{NA}.
\item \strong{removed: below threshold} — a matching layer exists, the value is
non-\code{NA} but \verb{<= min_abundance} (only possible when \code{min_abundance > 0}).
\item \strong{unassessed: no range map} — no matching layer (non-birds such as
insects/mammals/anthropogenic labels, and birds absent from the
stack, e.g. arid specialists). These are always \strong{kept}.
}
Only species in the "removed" categories are dropped, so the function never
removes a species it cannot assess.
}
\examples{
\dontrun{
r <- terra::rast("nsw_abundance_stack_3km.tif")
d <- read_birdnet_folder("wilddeserts")
filtered <- filter_by_range(d, r, latitude = -29.23, longitude = 141.29)
attr(filtered, "range_report")
}
}
