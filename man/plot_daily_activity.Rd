% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_daily_activity.R
\name{plot_daily_activity}
\alias{plot_daily_activity}
\title{Plot daily (diel) activity heatmap}
\usage{
plot_daily_activity(
  df,
  site = NULL,
  species = NULL,
  confidence = 0.5,
  date = NULL,
  min_detections = 1,
  exclude = NULL,
  group_col = NULL,
  group_colours = NULL,
  lat = NULL,
  lon = NULL,
  tz = "UTC",
  bin_min = 20,
  layout = c("rows", "columns"),
  title = NULL,
  subtitle = NULL,
  fill_palette = NULL
)
}
\arguments{
\item{df}{A data frame of BirdNET results, ideally from \code{read_birdnet_folder}
or \code{read_birdnet_sites}. Must contain \code{recording_window_time}, \verb{Common Name}
and \code{Confidence}. If a \code{Site} column is absent it is derived from the
\code{file_name} prefix (everything before the \verb{_YYYYMMDD_HHMMSS} stamp).}

\item{site}{Optional character vector of site names to include. \code{NULL}
(default) keeps every site; each site becomes its own facet panel.}

\item{species}{Optional character vector of \verb{Common Name} values to include.
\code{NULL} (default) keeps every species.}

\item{confidence}{Numeric. Minimum BirdNET confidence (0-1) to retain.
Defaults to 0.5 (below this is mostly false positives).}

\item{date}{Controls the time window. \code{NULL} (default) averages every day in
the recording, folding them onto one 24-hour clock (each species row scaled
to its own peak). Supply a single date (a \code{Date} or "YYYY-MM-DD" string) to
instead plot just that one 24-hour period.}

\item{min_detections}{Integer. Drop any species with fewer than this many
detections (after the confidence/site/date filters) so the figure is not
cluttered by one-off detections. Counted per site, so a species is shown
wherever it clears the threshold. Defaults to 1 (keep everything).}

\item{exclude}{Optional character vector of \verb{Common Name} values to drop
(e.g. environment/noise classes or unreliable species).}

\item{group_col}{Optional name of a column that classifies each species into
a group (e.g. \code{"group"} with values like Birds / Insects). When supplied,
the y-axis species labels are coloured by group.}

\item{group_colours}{Optional named vector mapping group -> colour (e.g.
\code{c(Birds = "#3b82c4", Insects = "#e9a33b")}). Groups without an entry fall
back to grey. Ignored unless \code{group_col} is set; defaults to a hue palette.}

\item{lat, lon}{Optional latitude/longitude. When both are supplied (and the
\code{suncalc} package is installed) the representative sunrise/sunset for the
deployment is shaded as night bands, and species are ordered into a
nocturnal block then a diurnal block.}

\item{tz}{Time zone used to convert sunrise/sunset to local time of day.
Defaults to "UTC".}

\item{bin_min}{Numeric. Time-of-day bin width in minutes. Defaults to 20.}

\item{layout}{Site panel arrangement: \code{"rows"} (default) stacks sites
vertically, each panel repeating the species axis; \code{"columns"} places sites
side-by-side sharing a single species axis (no repetition).}

\item{title, subtitle}{Optional plot title / subtitle. When \code{NULL} (default)
sensible text is generated from \code{date} and whether night shading is drawn.}

\item{fill_palette}{Optional vector of colours for the activity fill, passed
to \code{\link[ggplot2:scale_fill_gradientn]{ggplot2::scale_fill_gradientn()}}, lightest first. Defaults to \code{NULL},
meaning the viridis "magma" ramp. Pass \code{\link[=birdnet_palette]{birdnet_palette()}}'s sequential
colours to match the rest of a deployment report.}
}
\value{
A \code{ggplot} object. Save it with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}; using
\code{device = ragg::agg_png} is recommended for crisp text.
}
\description{
Folds every recording day onto a single 24-hour clock and draws a heatmap of
calling activity by time of day, one row per species and one panel per site.
Each species row is scaled to its own peak so the plot shows \emph{when} a species
calls rather than how often. This is useful for comparing diel (within-day)
vocal patterns across species and sites.
}
\details{
Times are folded by taking seconds since local midnight from
\code{recording_window_time} and carrying them on an arbitrary reference date so
the x-axis is a clean 24-hour time axis. Species are ordered by daylight
fraction (when sunrise/sunset are known) and then by the circular mean of
their call time, so the midnight wrap-around does not scatter night-active
species.
}
\examples{
\dontrun{
d <- read_birdnet_sites(c("detections_SiteA", "detections_SiteB"))

# One site
plot_daily_activity(d, site = "SiteA", lat = -33.4, lon = 151.4,
                    tz = "Australia/Sydney")

# All sites at once, faceted
plot_daily_activity(d)

# A single 24h period from a longer deployment, selected species only
plot_daily_activity(d, site = "SiteA", date = "2024-01-15",
                    species = c("Laughing Kookaburra", "Grey Fantail"))
}
}
