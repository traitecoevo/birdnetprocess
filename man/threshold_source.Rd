% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{threshold_source}
\alias{threshold_source}
\title{Where each species' threshold came from}
\usage{
threshold_source(cfg, species)
}
\arguments{
\item{cfg}{A \code{birdnet_deployment}, from \code{\link{read_deployment}}.}

\item{species}{Character vector of common names.}
}
\value{
A character vector parallel to \code{species}.
}
\description{
Labels each species \code{"calibrated"}, \code{"unvalidated"} or \code{"default"}, so a
report can say which detections rest on a measured operating point and which
rest on an assumption.
}
\details{
The distinction that matters is between a species that was \emph{never tested} and
one that was \emph{tested and failed}. Both end up on the deployment's default
threshold, but they mean opposite things: the first is simply unknown, while
the second is a species the evaluation showed the detector could not
identify reliably at any cut. Collapsing them into one bucket quietly
launders a known-bad species into a plain result, so
\code{analysis$unvalidated_species} names the second group explicitly and the
report flags it.
}
\seealso{
\code{\link{species_thresholds}}
}
