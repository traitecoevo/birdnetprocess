% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{birdnet_palette}
\alias{birdnet_palette}
\title{Categorical colours for plotting}
\usage{
birdnet_palette(n = NULL, all_pairs = FALSE)
}
\arguments{
\item{n}{Number of colours. Defaults to the whole palette.}

\item{all_pairs}{Use the stricter all-pairs-safe set? Default \code{FALSE}.}
}
\value{
A character vector of hex colours, named by hue.
}
\description{
Returns \code{n} colours from the package's categorical palette, in a fixed
order. The order is the point: it was chosen so that neighbouring series
stay distinguishable to readers with colour-vision deficiency, so take the
colours from the front rather than picking favourites.
}
\details{
Two orders are available. The default suits series that are read against
their neighbours - bars, stacked areas, lines. \code{all_pairs = TRUE} returns a
smaller set that holds up when \emph{every} pair may be compared at once, such as
coloured species labels scattered down a figure; past four such colours, no
ordering of this palette separates reliably, so fold the rest into a
residual group instead of asking for more.
}
\examples{
birdnet_palette(3)
birdnet_palette(4, all_pairs = TRUE)
}
