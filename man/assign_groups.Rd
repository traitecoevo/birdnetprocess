% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxon_groups.R
\name{assign_groups}
\alias{assign_groups}
\title{Sort detections into taxon groups}
\usage{
assign_groups(
  data,
  labels = NULL,
  rules = NULL,
  species_col = "Common Name",
  group_col = "group"
)
}
\arguments{
\item{data}{A tibble of detections, e.g. from
\code{\link{read_deployment_detections}}.}

\item{labels}{Path to a labels file, or a tibble from
\code{\link{read_detector_labels}}. If \code{NULL}, taken from the
deployment config attached to \code{data}.}

\item{rules}{A named list of grouping rules. If \code{NULL}, taken from
\code{analysis$groups} in the attached config.}

\item{species_col}{Column holding the common name. Default \code{"Common Name"}.}

\item{group_col}{Name of the column to add. Default \code{"group"}.}
}
\value{
\code{data} with an added \code{group_col} column, a factor whose levels
follow the order the groups were declared in.
}
\description{
Adds a \code{group} column to a detection table - \code{"Birds"}, \code{"Insects"},
\code{"Frogs"} and so on - by matching each species' genus, taken from the
detector's labels file, against the rules in a deployment config. Use it to
colour or facet a plot by broad taxon, as
\code{\link{plot_daily_activity}}'s \code{group_col} argument expects.
}
\details{
A custom classifier covering more than birds gives no direct signal about
what a class \emph{is}; the genus in the labels file is the signal. Rules are
written per deployment because the relevant split differs by project -
cicadas versus birds in one, native versus introduced in another.

Each rule may list \code{genera} (matched against the genus), \code{species} (matched
against the common name, for one-off cases the genus can't express), or
\code{default: true}. Exactly one group should be the default; everything
unmatched lands there. Rules are applied in the order written, so an earlier
group wins a tie.
}
\examples{
\dontrun{
cfg <- read_deployment("deployments/smiths_lake/deployment.yml")
d <- read_deployment_detections(cfg) |> assign_groups()
table(d$group)
}
}
\seealso{
\code{\link{read_detector_labels}}, \code{\link{group_colours}}
}
