% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{species_thresholds}
\alias{species_thresholds}
\title{Per-species detection thresholds}
\usage{
species_thresholds(cfg, species)
}
\arguments{
\item{cfg}{A \code{birdnet_deployment}, from \code{\link{read_deployment}}.}

\item{species}{Character vector of common names.}
}
\value{
A numeric vector of thresholds, parallel to \code{species}.
}
\description{
Returns the confidence threshold that applies to each of \code{species}, taking a
species' own calibrated cut where the config has one and the deployment-wide
\code{analysis$confidence} otherwise.
}
\details{
A single global threshold treats every species as equally detectable, which
no evaluation ever supports: a distinctive call and a faint, easily-confused
one need different cuts to reach the same precision. Where a labelled
soundscape has been scored, \code{analysis$confidence_by_species} carries the
per-species operating points; everything else falls back to
\code{analysis$confidence}.

Thresholds must be in the same space as the detections' \code{Confidence} column.
Evaluation harnesses often work in \strong{logits} while detectors emit
post-sigmoid \strong{probabilities} — a threshold moved between the two without
converting looks entirely plausible and silently passes almost everything.
Convert before putting values in a config.
}
\examples{
\dontrun{
cfg <- read_deployment("deployments/wild_deserts/deployment.yml")
species_thresholds(cfg, c("Brown Songlark", "Budgerigar"))
}
}
\seealso{
\code{\link{threshold_source}}
}
