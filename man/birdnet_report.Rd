% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/birdnet_report.R
\name{birdnet_report}
\alias{birdnet_report}
\title{Render a deployment report}
\usage{
birdnet_report(
  cfg,
  output_dir = NULL,
  data = NULL,
  filename = NULL,
  quiet = TRUE
)
}
\arguments{
\item{cfg}{A \code{birdnet_deployment}, from \code{\link{read_deployment}}, or a
path to a \code{deployment.yml}.}

\item{output_dir}{Directory for the report. Defaults to a \verb{report/} folder
beside the config file.}

\item{data}{Optional pre-read detections. If \code{NULL}, read from the config.}

\item{filename}{Output file name. Defaults to the deployment name, slugified.}

\item{quiet}{Suppress Quarto's render output? Default \code{TRUE}.}
}
\value{
The path to the rendered HTML, invisibly.
}
\description{
Turns a deployment config into a single self-contained HTML report: what was
recorded, where, with which detector, and what it heard. Every figure is
also written as a standalone PNG for reuse in talks and papers.
}
\details{
Everything the report needs comes from the config, so the same command
regenerates the report after more recordings arrive or the threshold
changes. Detections are read via \code{\link{read_deployment_detections}}
unless you pass them in - useful if you want to modify them first.

Figures are rendered on a light background regardless of the reader's
theme, since a PNG cannot restyle itself for a dark page.

Requires the \code{quarto} R package and a Quarto installation.
}
\examples{
\dontrun{
cfg <- read_deployment("deployments/smiths_lake/deployment.yml")
birdnet_report(cfg)
}
}
\seealso{
\code{\link{read_deployment}}, \code{\link{site_report}} for the
simpler single-site PNG output.
}
