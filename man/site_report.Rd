% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_report.R
\name{site_report}
\alias{site_report}
\title{Build a per-site report and plot set from BirdNET detections}
\usage{
site_report(
  data,
  latitude,
  longitude,
  abundance_raster = NULL,
  confidence = 0.5,
  output_dir = NULL,
  site_name = NULL,
  n_top_species = 10,
  tz = "Australia/Sydney",
  start_after = NULL,
  end_before = NULL,
  min_abundance = 0,
  name_overrides = NULL,
  keep_species = NULL
)
}
\arguments{
\item{data}{A tibble of BirdNET detections (e.g. from
\code{\link{read_birdnet_folder}}).}

\item{latitude, longitude}{Numeric. The recorder location (decimal degrees,
WGS84). Used both for range filtering and for day/night shading.}

\item{abundance_raster}{Optional \code{terra} \code{SpatRaster} (or path) of per-species
abundance layers. When supplied, detections are range-filtered via
\code{\link{filter_by_range}}.}

\item{confidence}{Numeric. Minimum confidence for the summary and plots.
Default \code{0.5}.}

\item{output_dir}{Optional directory to write the report CSV and plot PNGs
to. Created if it does not exist. If \code{NULL}, nothing is written.}

\item{site_name}{Optional label for the site (used in messages / file names).}

\item{n_top_species}{Number of top species for the activity plots. Default \code{10}.}

\item{tz}{Time zone for day/night shading. Default \code{"Australia/Sydney"}.}

\item{start_after, end_before}{Optional deployment-window bounds. Detections
with \code{recording_window_time} before \code{start_after} or after
\code{end_before} are dropped. Useful when the recorder was running before
it was placed at the site (e.g. test recordings made elsewhere). May
be a \code{POSIXct}, a \code{Date}, or a string such as \code{"2026-05-26"} (parsed
in UTC, matching the times produced by \code{\link{read_birdnet_file}}).}

\item{min_abundance}{Passed to \code{\link{filter_by_range}}. Default \code{0}.}

\item{name_overrides}{Passed to \code{\link{filter_by_range}}.}

\item{keep_species}{Passed to \code{\link{filter_by_range}}: common names to
always keep regardless of modelled range (e.g. nomadic waterbirds).}
}
\value{
Invisibly, a list with elements \code{report} (per-species filter report,
or \code{NULL} if no raster), \code{summary} (the
\code{\link{summarise_detections}} tibble), \code{plots} (named list of
ggplot objects), \code{data_filtered} (the filtered detections), and
\code{output_dir}.
}
\description{
A convenience wrapper that produces a standard set of outputs for a single
recording site: optionally range-filters the detections (removing geographic
false positives), summarises them, and builds two plots — a species-counts
bar chart and a top-species activity plot with day/night shading. When
\code{output_dir} is supplied, the plots and a species-filter report are written
to disk.
}
\details{
The work is delegated to existing package functions:
\code{\link{filter_by_range}}, \code{\link{summarise_detections}},
\code{\link{plot_species_counts}}, and \code{\link{plot_top_species}}. If
\code{abundance_raster} is \code{NULL}, no range filtering is performed and all
detections are reported.
}
\examples{
\dontrun{
r <- terra::rast("nsw_abundance_stack_3km.tif")
d <- read_birdnet_folder("wilddeserts")
res <- site_report(
  d,
  latitude = -29.229286, longitude = 141.286856,
  abundance_raster = r, tz = "Australia/Sydney",
  output_dir = "wilddeserts_plots/filtered"
)
res$report
}
}
